export const SECOND_IN_MS = 1000 as const;
export const MINUTE_IN_SECONDS = 60 as const;
export const MINUTE_IN_MS = SECOND_IN_MS * MINUTE_IN_SECONDS;
export const HOUR_IN_MINUTES = 60 as const;
export const HOUR_IN_MS = MINUTE_IN_MS * HOUR_IN_MINUTES;
